<Page loaded="loaded">
	<StackLayout orientation="vertical">
		<ListView items="{{ inputFields }}">
			<ListView.itemTemplate>
				<!-- <Label text="{{ name }}" /> -->
				<TextField hint="{{ helpText }}" id="{{ name }}" />
			</ListView.itemTemplate>
		</ListView>
	</StackLayout>
</Page>